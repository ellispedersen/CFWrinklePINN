# RunPod dependencies — PyTorch and ROCm/CUDA are provided by the Docker image.
# Install with: pip install -r requirements_runpod.txt
# Do NOT pin torch/torchvision here; they come from the container.

h5py>=3.8
numpy>=1.24
scipy>=1.10
tqdm>=4.65
